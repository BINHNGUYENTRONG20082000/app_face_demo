# Identity VM App — Python dependencies
# GPU: cài onnxruntime-gpu + torch CUDA phù hợp với driver của bạn.
# CPU-only: thay onnxruntime-gpu bằng onnxruntime nếu không có GPU.

# Web API
fastapi>=0.100.0
uvicorn[standard]>=0.22.0
pydantic>=2.0.0
python-multipart>=0.0.6
aiofiles>=23.0.0
requests>=2.31.0

# UI
streamlit>=1.28.0
pillow>=9.0.0

# Vision / ML
opencv-python>=4.8.0
numpy>=1.24.0
scipy>=1.10.0
insightface>=0.7.3
onnxruntime-gpu>=1.16.0
scikit-image>=0.21.0
scikit-learn>=1.3.0
ultralytics>=8.0.0
torch>=2.0.0

# Optional: faster face search (set IVM_USE_FAISS=1)
faiss-cpu>=1.8.0

# Dev / test
pytest>=7.0.0
